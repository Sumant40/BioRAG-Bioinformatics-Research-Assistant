# render.yaml
services:
  - type: web
    name: bio-rag-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python run.py
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: NCBI_API_KEY
        sync: false